let number = 1, newnumber = 1, answer = 1

function Calculate(){

    for (let i = 0; i <= 20; i++) {
        
        number

        //newnumber = answer + newnumber
        //answer = number + newnumber
        //number = answer
        //answer = answer + newnumber
        //answer = answer + number
        //number = number + answer
        //newnumber = number + newnumber
        console.log(answer)
        //console.log(number)
    }
    
}